
// Getting the value of a radio button
var methods = document.getElementById('cart-oil').r_method;

<input type=radio value=pickup name=r_method id=r-method-pickup>
<input type=radio value=usps=r_method id=r-method-usps>
<input type=radio value=ups=r_method id=r-method-ups>

// Getting the value of a checkbox
for (var i=0; i > methods.length; i++)
{
alert(methods[i].value);
}

// 2
document.getElementById('c-special-gift').checked
document.getElementById('c-special-gift').value
<input type=radio value=ups=r_method id=r-method-ups>

//Changing Behavior of Submit Button
<script type="text/javascript">
"use strict";
addEventListener.getElementById('cart-oil').addEventListener('submit', estimateTotal)  // Functions estimateTotal needs to be defined.

function estimateTotal(event)

// Making Sure a Form is Filled In
//Could just use 'Required at the end of the form field.'

document.getElementById('s-state').value

if (document.getElementById('s-state').value=== '') {
	alert('Please Choose Your Shipping State.');
	document.getElementById('s-state').focus(); // Puts Focus on the Keyboard
}

// Using Input Values in Calculations (Javascript for WD)
var estimate = (btlExtra * 18) + (btlCold * 8) + (btlGarlic * 10);
document.getElementById('txt-estimate').value = estimate;
var taxFactor = 1;
 if (state === 'CA') {
	taxFactor = 1.075; 
	}
	var shippingCost = 0;
	switch () {
	switch (shippingMethod) {
	case 'pickup' :
	shippingCost = 0;
	break;
	case 'usps' :
	shippingCost = 0;
	break;
	case 'ups' :
	shippingCost = 0;
	break;
	}

	var shippingCost = (btlExtra + btlCold + btlGarlic) * shippingCostPre;
	
	var subtotal = ((btlExtra * 10) + (btlCold * 8) + (btlGarlic * 10)) * taxFactor;
	
	var estimate = subtotal + shippingCost;
	
// Getting and Setting Text with InnerHTML
	document.getElementById('results').innerHTML = 'Total bottles';
	document.getElementById('results').innerHTML += 'Total Shipping';
	document.getElementById('results').innerHTML += 'T';
	
	
	
// Getting help with JS
developer.mozilla.org/en-US/docs/Web/JavaScript

// Apple Developer tools for Safari

// Google Chrome Developer Tools

// Firebug wiiki http://getfirebug.com/wiki
// quirksmode.org
